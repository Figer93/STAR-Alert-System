[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.frontend"
watchPatterns = ["/frontend/**"]
buildArgs = { VITE_API_SECRET_KEY = "${{VITE_API_SECRET_KEY}}" }

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"